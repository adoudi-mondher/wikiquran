# قرآن · شبكة المعرفة

> قاعدة بيانات تحليلية للقرآن الكريم، مُصوَّرة على شكل شبكة معرفية حيّة — مبنيّة كليًا على البيانات، دون أي تفسير ديني.

**🌐 الموقع :** [quranicdata.org](https://quranicdata.org)

---

<div align="center">

[العربية](./README.md) · [English](./README.en.md) · [Français](./README.fr.md)

</div>

---

## الرؤية والتوجّه

WikiQuran ليس مجرد عارض للقرآن الكريم. إنه أول أداة تتعامل مع القرآن بوصفه **مجموعة بيانات تحليلية** وتُصوِّره كـ**شبكة معرفية حيّة** — دون أي تفسير ديني، بل فقط الأنماط التي تكشفها البيانات.

### ما تكشفه الشبكة

- **البنية الدلالية الخفية** — الجذور العربية كشبكة عصبية للقرآن الكريم
- **التطور السردي** — الفروق اللغوية بين السور المكية والمدنية
- **الأنماط البنيوية** — التكرارات والروابط والتجمعات الموضوعية المستخرجة من البيانات

### المبادئ الثابتة

- ✅ **قابلية التوسع** — كل قرار تقني يسمح بالتطوير المستمر
- ✅ **الحياد** — الاستنتاجات تنبع من البيانات لا من المؤلفين
- ✅ **التحليلية** — القرآن يُعامَل كأي مدوّنة لغوية

---

## البيانات

**المصادر :** [tanzil.net](https://tanzil.net/download) / [corpus.quran.com](https://corpus.quran.com/download/default.jsp) *(بيانات مفتوحة)*

| العنصر | الحجم |
|--------|-------|
| السور | 114 |
| الآيات | 6 236 |
| الكلمات الفريدة | 12 113 |
| الجذور العربية | 1 642 |
| التكرارات | 77 429 |
| علاقات SHARES_ROOT | 6 035 766 |

### استراتيجية الاستخراج

| المصدر | الملف | المحتوى |
|--------|-------|---------|
| tanzil.net | `quran-uthmani.xml` | النص العربي (الرسم العثماني) |
| tanzil.net | `quran-data.xml` | البيانات الوصفية: ترتيب النزول، نوع السورة، الجزء، الحزب |
| corpus.quran.com | `morphology.txt` (TSV) | الجذور + الصرف كلمةً كلمة |

---

## معمارية البيانات

### المبدأ الأساسي

> **PostgreSQL هو المصدر الرئيسي. Neo4j هو فهرس مشتق من الرسم البياني.**

- لا نكتب **أبدًا** مباشرةً في Neo4j دون المرور عبر PostgreSQL
- إذا تلف Neo4j ← إعادة بناء كاملة من PostgreSQL
- كل عقدة في Neo4j تحمل `pg_id` ← جسر نحو مصدر الحقيقة

### الحدود بين ACID و BASE

| | PostgreSQL (ACID) | Neo4j (BASE) |
|--|-------------------|--------------|
| **الدور** | مصدر الحقيقة | محرك الاستكشاف |
| **الضمان** | معاملات صارمة | توفر + أداء |
| **البيانات** | النصوص، البيانات الوصفية، التكرارات | العلاقات، الروابط، الرسم البياني |
| **نوع السؤال** | *"ماذا تقول الآية 2:255؟"* | *"أي الآيات تتشارك هذه الجذور؟"* |

---

## نموذج PostgreSQL

### الجداول

```
surah ──< ayah ──< word_occurrence >── word >── root
```

| الجدول | الدور | المفاتيح الأساسية |
|--------|-------|------------------|
| `surah` | السور + البيانات الوصفية الكاملة | `revelation_order`، `type` |
| `ayah` | الآيات + النص العربي العثماني | `surah_id`، `number` |
| `root` | الجذور الفريدة | `buckwalter` (مفتاح)، `arabic` (عرض) |
| `word` | الكلمات الفريدة | `root_id`، `lemma_buckwalter`، `pos` |
| `word_occurrence` | كل ظهور لكلمة في آية | `word_id`، `ayah_id`، `position` |

### قرارات معمارية

- ✅ **كلمات فريدة** في `word` + تكرارات في `word_occurrence` (نمط many-to-many)
- ✅ **Buckwalter** كمفتاح تقني، **العربية** للعرض (تحويل عند الاستيراد)
- ✅ **بيانات السور الكاملة** منذ المرحلة 1 (جزء، حزب، صفحة المصحف)
- ⏭️ **الصرف في المرحلة 6** : جدول `morpheme` للبادئات واللواحق

---

## نموذج Neo4j

### العقد

| العقدة | الخصائص الأساسية | المصدر |
|--------|----------------|--------|
| `Surah` | `pg_id`، `number`، `type`، `revelation_order` | المرحلة 1 ✅ |
| `Ayah` | `pg_id`، `surah_number`، `ayah_number` | المرحلة 1 ✅ |
| `Word` | `pg_id`، `text_arabic`، `pos` | المرحلة 1 ✅ |
| `Root` | `pg_id`، `buckwalter`، `arabic`، `occurrences` | المرحلة 1 ✅ |
| `Person` | `pg_id`، `name_arabic`، `type` | المرحلة 6 ⏭️ |
| `Theme` | `pg_id`، `label_arabic` | المرحلة 6 ⏭️ |

### العلاقات

```cypher
(Surah)-[:HAS_AYAH]---------------------------------------->(Ayah)
(Ayah)-[:CONTAINS {position}]----------------------------->(Word)
(Word)-[:DERIVED_FROM]------------------------------------>(Root)
(Ayah)-[:SHARES_ROOT {root_bw, root_ar, count}]---------->(Ayah)   ← المفتاح التحليلي — المرحلة 1
(Ayah)-[:MENTIONS]----------------------------------------->(Person) ← المرحلة 6
(Person)-[:CO_MENTIONED {count}]-------------------------->(Person) ← المرحلة 6
(Ayah)-[:HAS_THEME]---------------------------------------->(Theme)  ← المرحلة 6
```

### العلاقة المميّزة : `SHARES_ROOT`

يرتبط آيتان إذا اشتركتا في جذر واحد على الأقل. `count` = عدد الجذور المشتركة (وزن العلاقة). هذا ما **لا تقدمه أي أداة أخرى** بصريًا.

```cypher
// مثال: الآيات الأكثر ارتباطًا بآية الكرسي
MATCH (a1:Ayah {surah_number: 2, ayah_number: 255})
      -[r:SHARES_ROOT]->(a2:Ayah)
RETURN a2.surah_number, a2.ayah_number, r.count AS shared_roots
ORDER BY shared_roots DESC
LIMIT 20;
```

---

## المكدّس التقني

### الواجهة الأمامية (Frontend)

| الأداة | الدور |
|--------|-------|
| **React 19 + Vite** | إطار عمل الواجهة |
| **TailwindCSS v4** | التنسيق (دعم RTL العربي) |
| **react-force-graph** | عرض الشبكة (WebGL) |
| **TanStack Query v5** | تخزين مؤقت لـ API |
| **TypeScript** | التحقق من الأنواع |

### الواجهة الخلفية (Backend)

| الأداة | الدور |
|--------|-------|
| **FastAPI** (Python 3.12) | REST API — 9 نقاط وصول |
| **SQLAlchemy 2.0** | ORM لـ PostgreSQL |
| **Pydantic v2** | التحقق من صحة البيانات |

### قواعد البيانات

| الأداة | الدور |
|--------|-------|
| **PostgreSQL 17-alpine** | النصوص، البيانات الوصفية، البحث النصي |
| **Neo4j 5.26 LTS Community** | شبكة العلاقات (+6 مليون SHARES_ROOT) |

### البنية التحتية

| البيئة | الأدوات |
|--------|---------|
| **التطوير** | Docker Compose |
| **الإنتاج** | VPS OVH Debian — Nginx Proxy Manager + Docker Compose |

---

## خارطة الطريق

| المرحلة | الوصف | الحالة |
|---------|-------|--------|
| المرحلة 1 | استخراج البيانات | ✅ مكتملة |
| المرحلة 2 | قاعدة البيانات | ✅ مكتملة |
| المرحلة 3 | Backend API | ✅ مكتملة |
| المرحلة 4 | الواجهة الأمامية | ✅ مكتملة |
| المرحلة 5 | النشر على VPS | ✅ مكتملة |
| المرحلة 6 | الإثراء والتطوير | ⏳ جارية |

التفاصيل الكاملة في [ROADMAP.md](./ROADMAP.md).

---

## هيكل المشروع

```
wikiquran/
│
├── backend/
│   └── app/
│       ├── main.py
│       ├── config.py
│       ├── database.py
│       ├── models/        # SQLAlchemy — نماذج البيانات
│       ├── schemas/       # Pydantic — مخططات التحقق
│       ├── api/           # Routes — نقاط الوصول
│       ├── services/      # منطق الأعمال
│       └── utils/         # أدوات مساعدة (buckwalter، إلخ)
│
├── frontend/
│   ├── public/            # favicon.svg، og-image.png
│   └── src/
│       ├── components/
│       ├── pages/
│       ├── hooks/
│       ├── api/
│       └── lib/
│
├── scripts/
│   ├── extraction/        # محللات Tanzil + Corpus Quran
│   ├── database/          # استيراد PG + مزامنة Neo4j
│   └── utils/             # Buckwalter → عربي، أدوات مساعدة
│
├── data/
│   ├── quran_raw/         # الملفات المصدر الخام
│   └── quran_enriched/    # JSON المعياري الوسيط
│
├── schema_postgresql.sql
├── docker-compose.yml
├── docker-compose.prod.yml
├── ROADMAP.md
└── README.md
```

---

## الاعتبارات الأخلاقية

- ✅ بيانات عامة ومفتوحة (CC-BY 3.0 / GNU GPL)
- ✅ استخدام تعليمي وبحثي وصحفي
- ✅ لا تفسير ديني مضاف — بيانات خام فقط
- ✅ لا جمع لبيانات المستخدمين

---

**آخر تحديث :** 28 فبراير 2026
**الحالة :** ⏳ المرحلة 6 جارية
**الإصدار :** 0.4.0
**الموقع :** https://quranicdata.org
**المؤلف :** [Sidr Valley AI](https://mondher.ch)
